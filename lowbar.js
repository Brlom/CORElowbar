const _ = {};

_.identity = (x) => {
  return x;
};

_/* .take = (arr, n) => {
  if (n === 0 ) {
    let n = [];
  }
  n ? n : n = 1;
  const box = [];
  for(let i = 0; i < n; i++) {
    box.push(arr[i]);
  }
  return box;
}


module.exports = _; */
